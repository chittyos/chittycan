name: Governance Gates

on:
  pull_request:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  gates:
    uses: ./.github/workflows/reusable-governance-gates.yml
    secrets: inherit
